## 2023-10-27 - Form Accessibility
**Learning:** Found that custom form components like the CardPayment component were not properly linking labels and inputs using `htmlFor` and `id`.
**Action:** Always ensure custom form components correctly implement label-to-input association for accessibility.
